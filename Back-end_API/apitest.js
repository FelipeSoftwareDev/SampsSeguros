const readline = require("readline-sync");

const cpf = Array(Number(readline.question("CPF: ")));

array.forEach(cpfElements => {
    push.cpf;
});


function verifyCpf(){
    if(cpf.lenght != 9){
        return console.log("Erro, favor inserir um CPF válido.");
    }
    else{
        return
        const validCpf = cpf
    }
}

console.log(validCpf);