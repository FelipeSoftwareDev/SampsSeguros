window.onload = function(){

    const track = document.getElementById('slider-track');
    const image = document.getElementsByClassName('sliderImg')

}

let trackWidth = 0;
for (let img of images) {
    trackWidth +=img.width;
}

